# CPU stack. Use this for the correctness gate, plotting, and reduced-scale checks.
# It CANNOT produce comparable benchmark numbers — the drivers refuse to run a
# benchmark on the CPU backend unless you pass --allow-cpu. For the real run use
# requirements-gpu.txt (the only difference is the jax wheel).
#
# Python 3.12 / 3.13 / 3.14. Verified together by the full test suite (205 tests).
#
# NOTE on Windows without admin rights: pip fails extracting orbax-checkpoint because
# its nested test paths exceed the 260-char MAX_PATH limit. Workaround:
#   pip install "jax[cpu]==0.11.1" optax==0.2.8 msgpack tensorstore rich PyYAML typing_extensions treescope toolz
#   pip install --no-deps flax==0.12.9 cax==0.4.3
# On Linux/macOS (and WSL2) this file installs cleanly as-is.

jax[cpu]==0.11.1
flax==0.12.9
optax==0.2.8
cax==0.4.3
pillow

# reference implementation (migration-correctness gate only)
torch>=2.12,<3

# data / viz / io
numpy
# scipy provides the exact Wilcoxon signed-rank test used for every paired architecture
# comparison. pinca_jax.stats falls back to a sign test without it, which is strictly more
# conservative and would silently weaken the reported significance -- so it is a real
# dependency, not an optional one.
scipy
matplotlib
imageio

# report generation
reportlab

# dev
pytest
