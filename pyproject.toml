[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "pinca-jax"
version = "0.1.0"
description = "PI-NCA vs PINN vs neural-operator PDE benchmark suite (JAX)"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
